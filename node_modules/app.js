const express= require('express');
const app= express();
const d3= require('./jsDay3');

app.use((req,res,next)=>{
    res.setHeader('Content-Type','text/html');
    next();
});

app.get('/', (req, res) => {
    d3(4,0,4)
    res.send(d3)
    let bookCredit= d3(4,0,4);
    console.log(bookCredit);
    
  })

app.listen(3000);